# Take a sentence as input
sentence = input("Enter a sentence: ")

vowels = "aeiouAEIOU"
v_count = c_count = d_count = s_count = 0

# a) Count vowels, consonants, digits, and special characters.
for ch in sentence:
    if ch.isdigit():
        d_count += 1
    elif ch.isalpha():
        if ch in vowels:
            v_count += 1
        else:
            c_count += 1
    elif ch != " ":
        s_count += 1

print("Vowels:", v_count)
print("Consonants:", c_count)
print("Digits:", d_count)
print("Special Characters:", s_count)

# b) Print each word in reverse using a for loop.
words = sentence.split()
print("\nEach word in reverse:")
for w in words:
    print(w[::-1])

# c) Print the longest word from the sentence.
longest = ""
for w in words:
    if len(w) > len(longest):
        longest = w

print("\nLongest word:", longest)

# Program to count occurrence of each character.
s = "banana"
freq = {}
for ch in s:
    freq[ch] = freq.get(ch, 0) + 1
print(freq)

# Program to remove all vowels from a string.
s = "Data Science"
result = ""
for ch in s:
    if ch.lower() not in "aeiou":
        result += ch
print(result)

# String Operations
s = " Hello World "
print("Upper:", s.upper())
print("Lower:", s.lower())
print("Strip:", s.strip())
print("Find 'o':", s.find('o'))
print("Replace:", s.replace("World", "Python"))
print("Split:", s.split())
print("Title:", s.title())

isalpha()	letters only
isdigit()	numbers only
isalnum()	letters + numbers
isspace()	whitespace only
islower()	all lowercase
isupper()	all uppercase
istitle()	title format?

split()	"a,b,c".split(',') → ['a','b','c']
join()	" ".join(['hello','world'])

len()	length of string
sorted()	returns sorted characters
format()	"Hello {}".format("Faria")

